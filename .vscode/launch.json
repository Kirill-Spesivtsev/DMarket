{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Debug Angular app in browser",
            "url": "https://localhost:4200",
            "webRoot": "${workspaceFolder}/Dmarket.Client"
        }
    ]
}